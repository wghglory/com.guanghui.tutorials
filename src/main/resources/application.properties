spring.datasource.url=jdbc:postgresql://localhost:5433/conference
spring.datasource.username=postgres
spring.datasource.password=ca$hc0w
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Hibernate ddl auto (create, create-drop, validate, update)
# update means table will be created in the database automatically corresponding to defined data model.
# Any change to the model will also trigger an update to the table. For production, this property should be validate.
spring.jpa.hibernate.ddl-auto=update